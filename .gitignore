# dependencies
/node_modules
# 巢狀 node_modules 也要擋（assets/personality-atlases/ 曾在子目錄跑過 npm install）
node_modules/
/data

# AI 生成暫存輸出（325MB+，非版控素材；正式頭像成品在 public/images/personality-avatars/）
/outputs/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/
next-env.d.ts

# production
/build
/dist

# misc
.DS_Store
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
.env
.env*.local
!.env.example

# typescript
*.tsbuildinfo

# Payload 產出
src/payload-types.ts
/media
/public/media

# IDE
.vscode/*
!.vscode/settings.json
!.vscode/launch.json
!.vscode/extensions.json
.idea

# Claude Code — per-user local settings (tool permission toggles etc.)
# Keep .claude/launch.json tracked (team-shared preview config)
.claude/settings.local.json
# Claude Code session worktrees — 工作副本，不該進版控
.claude/worktrees/

# OS
Thumbs.db

# local tmp / debug artifacts (investigation scratch files)
.tmp-*
.tmp-crawl/

# handoff docs (optional — remove this line if you want PHASE*_HANDOFF.md tracked)
# PHASE*_HANDOFF.md
